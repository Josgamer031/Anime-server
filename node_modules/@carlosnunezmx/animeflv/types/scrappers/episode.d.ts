export type Server = {
    server: string;
    title: string;
    ads: number;
    allow_mobile: boolean;
    code: string;
};
export type Resources = {
    SUB: Server[];
    LAT?: Server[];
};
export declare function GetResources(Query: string): Promise<Resources>;
