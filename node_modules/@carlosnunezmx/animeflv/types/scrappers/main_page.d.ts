import { HTMLElement } from "node-html-parser";
export type HomeElement = {
    Id: string;
    Image: string;
    Type: string;
    Title: string;
    Review: number;
    Description: string;
};
export interface Episode extends Omit<HomeElement, "Description" | "Review" | "Type"> {
    Anime: string;
}
export declare function GetHome(): Promise<{
    Series: HomeElement[];
    Episodes: Episode[];
}>;
export declare function GetNewSeries(request?: HTMLElement): Promise<HomeElement[]>;
/**
 *
 * @param {import("node-html-parser").HTMLElement?} request
 */
export declare function GetNewEpisodes(request: HTMLElement): Promise<Episode[]>;
